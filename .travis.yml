language: python
python:
  - "2.7"
# command to install dependencies
install:
  - "pip install -U -r requirements.txt"
  - "pip install -e ."
  - "pip install -U pytest pytest-cov codecov"
# command to run tests
script: py.test --cov=knowledge_base -vv
after_success:
- codecov
